{
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^17.2.3",
    "express": "^5.1.0",
    "mysql2": "^3.15.3"
  },
  "scripts": {
    "start": "dotenv -e .env -- nodemon --delay 1 src/index.js",
    "dev": "nodemon src/index.js",
    "build": "node --check src/index.js && echo '✓ Backend syntax validated'",
    "test": "echo \"Error: no test specified\" && exit 1"
},
  "devDependencies": {
    "dotenv-cli": "^11.0.0",
    "@types/node": "^24.10.1",
    "nodemon": "^3.1.11"
}
}
